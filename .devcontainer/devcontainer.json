{
    "name": "Python 3",
    "image": "mcr.microsoft.com/devcontainers/python:3",
    "features": {},
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-python.vscode-pylance"
            ]
        }
    },
    "postCreateCommand": "pip install --upgrade pip && pip install -r requirements.txt",
    "postStartCommand": "sudo apt-get update && sudo apt-get install -y libgl1-mesa-glx libglib2.0-0"
}